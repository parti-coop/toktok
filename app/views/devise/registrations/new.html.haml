%section#sign-up
  .container
    %h3.sign-up-header
      당신의 멋진 아이디를 만들어주세요.
    .sign-up-form
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = devise_error_messages!
        = f.hidden_field :remember_me
        .form-group
          = f.label :nickname do
            사용할 아이디를 넣어 주세요.
          = f.text_field :nickname, autofocus: true, class: 'form-control'
          .text-primary
            * 한글, 알파벳, 숫자, _ 만 가능합니다.

        - if resource.email.blank? or resource.errors.include?(:email)
          .form-group
            = f.label :email do
              이메일을 넣어 주세요.
            = f.text_field :email, autofocus: true, class: 'form-control'

        .form-group
          = f.submit '계속하기', class: 'btn btn-primary btn-block', data: { disable_with: "진행 중..." }

        = form_tag nil, method: :get do
          = hidden_field_tag(:after_login)
          %label.remember_me
            "계속하기"를 클릭하면
            %a.text-primary{href: '/terms', target: '_blank'}
              %b 이용약관
            ,
            %a.text-primary{href: '/privacy', target: '_blank'}
              %b 개인정보처리
            방침 내용을 이해하고
            동의하게 됩니다.
